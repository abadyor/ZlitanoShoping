@* @page "/"
@using APPL.DTOs.Response.Category

<PageTitle>Home</PageTitle>



<!-- Categories List in Horizontal Cards -->
<div class="container mt-5">
    @if (Categorys != null && Categorys.Any())
    {
        @foreach (var item in Categorys)
        {
            <!-- Horizontal Card -->
            <div class="card mb-3">
                <div class="row g-0 d-flex align-items-center">
                    <!-- Image Section -->
                    <div class="col-md-2">
                        <img src="/css/images/food.png" class="img-fluid rounded-start" alt="@item.Name" style="height: 100px; width: 100px; object-fit: cover;">
                       <!---- <img src="item.ImageUrl" class="img-fluid rounded-start" alt="item.Name" style="height: 100px; width: 100px; object-fit: cover;">-->
                    </div>
                    <!-- Text Section -->
                    <div class="col-md-10">
                        <div class="card-body">
                            <h5 class="card-title">@item.Name</h5>
                            <p class="card-text">@item.Description</p>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <em class="text-success text-center fw-bold shadow-lg border-1">Loading data...</em>
    }
</div>
@code {
    private IEnumerable<CategoryGetDto> Categorys { get; set; } = [];

    protected override async Task OnInitializedAsync()
    {
        // Replace this line with actual service call
        Categorys = await categoryServices.GetAllAsync();
    }
} *@